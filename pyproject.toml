[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agenwiki"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
agenwiki-mcp = "agenwiki.mcp_server:main"
